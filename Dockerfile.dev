FROM node:20-alpine

WORKDIR /app

# Step A: Use a more stable registry mirror
RUN npm config set registry https://registry.npmmirror.com

COPY package*.json ./

# Step B: Increase timeout even further
RUN npm install --network-timeout=600000

COPY . .

EXPOSE 3006

CMD ["npm", "run", "start:dev"]